// 1. Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with there type.


let str ="string value";
let bool = true;
let nullval =null;
let und= undefined;

console.log(`${str} -> typeof ${typeof str}`);
console.log(`${bool} -> typeof ${typeof bool}`);
console.log(`${nullval} -> typeof ${typeof nullval}`);
console.log(`${und} -> typeof ${typeof und}`);
